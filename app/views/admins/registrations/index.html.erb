<h1>Manage Admins</h1>

<table>
  <tr>
    <th>Admin</th>
    <th>Email</th>
    <th>Super?</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admins.each do |admin| %>
  <tr>
    <td><%= admin.name %></td>
    <td><%= admin.email %></td>
    <td><%= admin.super %></td>

    <td><%= link_to 'Show', '#' %></td>
    <td><%= link_to 'Edit', 'edit' %></td>
    <td><%= link_to 'Destroy', '#', :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add Admin', 'new' %>
