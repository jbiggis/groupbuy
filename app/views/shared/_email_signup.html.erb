

<%= form_for :email, @email, :url => {:controller => "/emails", :action => "create" } do |f| %>

	<span class = "field">
		
		<%=t :email_sign_up%>  

		<%= f.text_field :email, :class => "email-input" %>


	</span>

	<span class = "action subscribe">


		<%=f.submit "Subscribe" %>

	</span>
<% end %>
 
