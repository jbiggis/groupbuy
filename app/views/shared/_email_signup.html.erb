

<%= form_for :email, @email, :url => {:controller => "/emails", :action => "create" } do |f| %>

	<div class = "field">
		<%= f.label :email %><br/>
		<%= f.text_field :email %>

	</div>

	<div class = "action">


		<%=f.submit "Subscribe" %>

	</div>
<% end %>
 
